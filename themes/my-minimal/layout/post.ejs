

<% if (page.notebook) { %>
    <%- observable_notebook(page.notebook, page.hideAfter || -1) %>
  
  <% } else { %>
    <h2><%- page.title %></h2>
  <% } %>
  
  <%- page.content%>
  <p><%- list_tags(page.tags, {class: 'classtest', show_count: false, style: false}) %> — <%- full_date(page.date, 'll') %></p>
  
  <br/>
  
  
  <% if (page.notebook) { %>
    <%- css('css/observable.css') %>
  <% } %>
  